{
  "private": true,
  "scripts": {
    "format": "elm-format src",
    "start": "elm-doc-preview",
    "test": "elm-analyse && elm-format --validate src && elm make --docs=/tmp/docs.json"
  },
  "devDependencies": {
    "elm-test": "^0.19.1-revision2",
    "elm-analyse": "^0.16.5",
    "elm-doc-preview": "^3.0.4",
    "elm-format": "^0.8.3"
  },
  "dependencies": {
    "elm": "^0.19.1-3"
  }
}
